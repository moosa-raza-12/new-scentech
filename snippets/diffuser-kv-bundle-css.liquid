<style>
  .kv-bundle-product-card{
    padding-right: 12px;
  }
  .kv-progress-footer2{
 /* border-radius: 12px 12px 0 0; */
 background: rgba(0, 0, 0, 0.10);
   padding: 10px 12px;
   text-align: center;
   margin: 8px -16px -12px;
  }
 @media (max-width: 639px) {
   .scent-badge{
    padding: 6px 12px !important;
  font-size: 10px !important;
  white-space: nowrap !important;
  }
  .step-circle{
  width: 24px !important;
  height: 24px !important;
  font-size: 12px !important;
  }
  .scent-title{
   font-size: 16px !important;
  }
  .scent-subtext{
   font-size: 12px !important;
   margin: 0;
   margin-top: 12px !important;
  }
  .kv-bundle-product-badge{
   display: none !important;
  }
  .kv-bundle-products-grid {
  grid-template-columns: repeat(1, 1fr);
  }
  .kv-bundle-product-image{
  width: 60px;
  height: 60px;
  margin: 0;
  }
  .kv-bundle-product-card{
  display: flex;
  padding: 6px;
  gap: 12px;
  }
  .kv-bundle-product-description{
  margin: 0;
  }
  .kv-bundle-product-title-wrapper{
  display: flex;
  align-items: center;
  flex-direction: column;
  align-items: start;
  justify-content: center;
  /* gap: 12px; */
  margin-left: 8px;
  }
  .kv-bundle-add-to-cart{
  min-width: 83px;
  }
  }
   .scent-header-content{
  display: flex;
  gap: 10px;
   align-items: center;
  } 
  @media (max-width: 768px) {
   .scent-header-content{
  gap: 8px;
  }   
  }
  
  .kv-progress-banner{
  max-width: none !important;
  }
  .scent-selector {
  padding-bottom: 20px;
  background: {{ section.settings.bg_color }};
  border-radius: 12px;
  font-family: inherit;
  }
  
  .scent-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
  }
  .kv-bundle-hero-section.sticky{
    position: fixed;
  }
  
  .step-circle {
  width: 34px;
  height: 34px;
  background: #162518;
  color: {{ section.settings.circle_text }};
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 600;
  font-size: 16px;
  }
  
  .scent-title-wrapper {
  flex: 1;
  }
  
  .scent-title {
  margin: 0;
  font-size: 20px;
  color: {{ section.settings.heading_color }};
  font-weight: 400;
  }
  
  .scent-subtext {
  margin: 4px 0 0;
  font-size: 16px;
  color: {{ section.settings.subtext_color }};
  }
  
  .scent-badge {
  background: {{ section.settings.badge_bg }};
  color: {{ section.settings.badge_text_color }};
  padding: 6px 12px;
  border-radius: 10px;
  font-size: 14px;
  white-space: nowrap;
  }
  .cds-highlights{
   display: flex;
              gap:5px;
              align-items: center;
              justify-content: center;
              width: fit-content;
              margin:0 auto;
 }
 .cds-highlights p{
   color: #17561F;
            font-size: 12px;
            font-weight: 600;
 }
 price-value{
   color: #17561F;
            font-size: 12px;
            font-weight: 600;
 }
  /* CSS */
                      /* .disc-active-msg {
 display: none;
                      }
                      
                      .kv-bundle-product-card.has-quantity .disc-active-msg {
 display: block;
                      } */
  
  </style>